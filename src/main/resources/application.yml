server:
  port: 8081
  servlet:
    context-path: /boot

spring:
  application:
    name: project
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
##日志
#logging:
#  file: /app/pms/log/contract/${spring.application.name}.log
#  #TRACE < DEBUG < INFO < WARN < ERROR < FATAL
#  level:
#    root: error
#    com:
#      infoepoch: debug
